## Packages
canvas-confetti | Celebration effect when coins are earned
@types/canvas-confetti | Types for canvas-confetti
framer-motion | Page transitions and UI animations
lucide-react | Icons for the interface

## Notes
PWA requires serving manifest.json and service-worker.js from the public directory.
Backend uses HTTP-only cookies for auth, so credentials: "include" is required for all fetches.
